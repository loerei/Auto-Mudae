Codebase is Python-heavy with small TypeScript/React frontend. Python files use type hints and fairly direct function-oriented style; tests are pytest unit tests under tests/unit. Keep Windows-native execution for this repo. Prefer semantic reads with Serena first, then narrow rg-based reads. Use apply_patch only for small localized edits; large patches on this workstation can hit Windows command-length issues, so split them or pivot to other edit strategies. For WebUI work, preserve local-only localhost behavior and portable clone-friendly setup.