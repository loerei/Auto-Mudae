After WebUI/backend changes, run at least: `cd webui && npm run build`, targeted pytest for touched units, and a backend smoke path using FastAPI TestClient or direct import. For launcher/startup changes, verify `run_webui.bat` syntax assumptions against cmd.exe rules; do not use shell heredoc syntax in batch files. For daemon lifecycle changes, verify startup/lifespan behavior, especially state reset and bootstrap import from existing .env/TOKENS_JSON.